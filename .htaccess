<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect root to public/index.php
    RewriteRule ^$ public/index.php [L]

    # Map static assets & routes into public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>




